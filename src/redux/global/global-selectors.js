export const countriesSelector = (state) => state.globalReducer.countries;

export const userLocationSelector = (state) => state.globalReducer.userLocation;

export const userLocationLoadingSelector = (state) => state.globalReducer.userLocationLoading;
